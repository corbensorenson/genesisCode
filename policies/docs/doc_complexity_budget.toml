version = 1

# Active docs exclude files marked with `Deprecated Top-Level Doc:`. R4.2.e's
# independently governed install and verify authorities add two normative contracts.
max_active_docs_md = 137

# Active top-level leaf docs exclude docs/INDEX.md and docs/DEPRECATION_MAP_v0.1.md.
max_active_top_level_docs_md = 6

# Average retrieval fan-out from feature matrix evidence paths.
max_capability_retrieval_fanout = 0.50
