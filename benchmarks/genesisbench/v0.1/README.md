# GenesisBench v0.1

Normative profile: `docs/spec/GENESISBENCH_PROTOCOL_v0.1.json`.

`contamination.fixture.json` is the run-bound evidence record for the public local-model conformance run. `eligibility.fixture.json` is its canonical deterministic classification. The result is intentionally unranked and declared-contaminated because the reference is public. Regenerate only by explicitly running the read-only protocol classifier with both records and review any byte change.

The signed append-only registry is R1.4.h. Temporal challenge payloads are R1.4.i. Neither is implied by this conformance fixture.
